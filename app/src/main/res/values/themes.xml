<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Mosam" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="fontFamily">@font/poppins</item>
    </style>

    <!-- Mosam theme   -->
    <style name="Theme.Mosam" parent="Base.Theme.Mosam" />

    <!-- App starting theme   -->
    <style name="Theme.Mosam.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/ic_launcher_background</item>
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Mosam</item>
    </style>
</resources>